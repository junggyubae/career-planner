# action/applications/ — per-target bundles

`<slug>/` holds `target.md` (analyzed target + frontmatter/status), `cv.{tex,pdf}`,
`sop.{tex,pdf}`, and `notes.md`. `BOARD.md` (derived) groups all targets by `status`.
